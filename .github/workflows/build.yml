name: Android Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 8
      uses: actions/setup-java@v4
      with:
        java-version: '8'
        distribution: 'adopt'
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
    - name: Grant execute permission for gradlew
      run: chmod +x C:/Users/OMEN/OneDrive/Documents/GitHub/CSI-Management-App/Android/gradlew
    - name: Build with Gradle
      run: C:/Users/OMEN/OneDrive/Documents/GitHub/CSI-Management-App/Android/gradlew build
    - name: Run tests
      run: C:/Users/OMEN/OneDrive/Documents/GitHub/CSI-Management-App/Android/gradlew test
